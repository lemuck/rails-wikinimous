<div class="container">

  <h1>Show</h1>
  <div class="article">
    <h1> <%= @article.title %> </h1>
    <p><%= @article.content %></p>
    <h3><%= link_to 'delete', article_path(@article), method: :delete, data: { confirm: "Delete #{@article.title}?"} %>  <%= link_to 'edit', edit_article_path(@article) %>
    	<%= link_to 'Home', articles_path %>
    </h3>
  </div>

</div>
